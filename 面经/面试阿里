
    代码从上往下执行

    不阻塞 页面加载

    加快加载页面 同步

    这样处理的好处是无需担心因页面未完成加载，造成DOM节点获取不到，使脚本报错的问题

    所有资源都加载完

    windowm.onload  等页面中所有资源加载完 包括图片 。。

    比刚才那个慢
    domContentLoad  不等资源加载完

    异步加载 js defer async

    vue-router 怎么只加载首屏js,

    切换路由的时候 切换组件

    vue data中数组中数据变了

    Object.defineProperty()

    去检测



    data 数据中 数组内容变了 怎么更新到view
    数组转成对象呢？

    双向数据绑定 看了点




    也有吧

    打包到js文件中

    没懂~


    怎么判断对象中是否有某个属性?
    hasownproperty   in  区别
    hasownproperty会检查原型下的属性，in不会


    webpack怎么打包的文件在浏览器中怎么运行


    media

    iframe 怎么请求后台数据 后台返回json数据


    跨域ajax post

    for in 主要遍历对象 ，遍历的是数组的索引（即键名）
    for of 主要遍历数组，遍历的是数组元素值。



    padding-top   200%
    但是外边要有个父元素
    padding-top相对于父元素的宽度

    左右有

    浮动 弹性盒模型
    display:inline-block;


    inline-block  float  脱离文档流
    inline-block  块级元素中间有空隙   空格 回车

    怎么清楚空隙

    没什么了

    擅长做一些实际项目 动手吧






    自己
    父元素

    入口 出口文件
    提取出来呢
    自己说

    离线缓存
    配置






    做个项目

    this不会
    自己说  有


    fastCLick  原理

    var  function哪个提升更高


    a();
    console.log(b);

    var b = 10

    function a(){
        console.log('111');
    }

    移动端点透

    transform  css3 跟哪个差别？

    动画 这个快吧  c3有gpu加速

    创建script标签 异步插入  会有async 异步加载

    ui

    一屏幕加载1000个



    变量提升 函数提升  函数会首先被提升，然后才是变量。
    css的加载不会阻塞DOM树解析  会阻塞DOM树的渲染
    https://www.cnblogs.com/chenjg/p/7126822.html









